ROUTETRACK VERCEL JOSE ESM RUNTIME FIX

Error fixed:
require() of ES Module .../jose/dist/webapi/index.js
from .../jwks-rsa/src/utils.js not supported

Replace this file in your existing RouteTrack project:

src/lib/firebase-admin.ts

Steps:

1. Extract this ZIP.
2. Copy firebase-admin.ts.
3. Paste it into:
   src/lib/
4. Choose "Replace the file in the destination."

Then run from the RouteTrack root folder:

npm run build

When successful:

git add src/lib/firebase-admin.ts
git commit -m "Fix Firebase authentication runtime on Vercel"
git push origin main

Wait for the new Vercel deployment to become Ready.

Open the newest deployment URL and test:

Bell > Enable phone alerts > Allow > Test notification

Important:
- Keep firebase-admin at version 14.1.0.
- Do not remove the VAPID key.
- No private-key change is required.
- No Firestore Rules change is required.
- NEXT_PUBLIC_FIREBASE_API_KEY must remain configured in Vercel.
